import React from "react";

function TypesPokemon({ type }) {
  return (
    <div className="container">
      {
      switch (type) {
        case'fire':
          img src=""/>
          break;
      case
        default:
          break;
      }
    }
        <
    </div>
  );
}
